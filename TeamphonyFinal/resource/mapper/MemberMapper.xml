<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="teamphony.store.mapper.MemberMapper">

	<insert id="insertMember" parameterType="Member">
		INSERT
		INTO MEMBER_TB
		(
		ID,
		PASSWORD,
		ALIAS,
		IMAGEPATH
		)
		VALUES
		(
		#{memberId},
		#{password},
		#{alias},
		#{imagePath}
		)
	</insert>
	
	<select id="selectMemberByMemberId" parameterType="String" resultType="Member">
		select id As memberId, password, alias, imagePath
		from member_tb
		where id = #{memberId}
	</select>
	
	<update id="updateMember" parameterType="Member">
		update Member_tb set password = #{password}, alias = #{alias}, imagePath = #{imagePath} where id = #{memberId}
	</update>

</mapper>